export interface CartItem {
  productId: string;
  name: string;
  price: number;
  slug: string;
  platform: string;
  region: string;
  edition: string;
  productImageUrl: string;
  quantity: number;
  userId: number;
}
