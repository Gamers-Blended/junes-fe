export interface ProductInWishlistDTO {
  id: string;
  name: string;
  slug: string;
  price: number;
  platform: string;
  region: string;
  edition: string;
  productImageUrl: string;
  createdOn: string;
}
